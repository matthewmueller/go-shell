# 0.1.0 / 2026-02-13

- **BREAKING:** `New(dir string)` is now `New()`. Set the working directory with `exec.Dir` after construction.
- add: `New()` now defaults `Exec.Dir` to `"."`.

# 0.0.1 / 2026-02-13

- Initial Release
